name: Release Act 1 - Core

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag'
        required: true
        type: string

jobs:
  release-act1:
    uses: kako-jun/.github/rust-cli-kiln/.github/workflows/release-act1.yml@main
    with:
      project-name: diffx
    secrets: inherit