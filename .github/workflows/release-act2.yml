name: Release Act 2 - Language Wrappers

on:
  workflow_run:
    workflows: ["Release Act 1 - Core"]
    types:
      - completed
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag to publish wrappers for'
        required: true
        type: string

jobs:
  release-act2:
    uses: kako-jun/.github/rust-cli-kiln/.github/workflows/release-act2.yml@main
    with:
      project-name: diffx
    secrets: inherit