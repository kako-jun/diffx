name: Release Build and Publish

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  release:
    uses: kako-jun/.github/.github/workflows/rust-cli-kiln-release.yml@main
    with:
      project-name: diffx
    secrets: inherit

