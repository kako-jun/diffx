# Ground Truth - 確認された真実

検証日時: 2025-11-14
最終更新: （実行後に記入）

## 🎯 このドキュメントについて

このファイルには、**実際に確認された動作のみ**を記録します。
推測・期待・希望は書きません。事実のみです。

## 📊 検証環境

```
OS: Linux (記録)
Rust: 1.88.0
Cargo: 1.88.0
Python: （記録）
Node.js: （記録）
```

## 🔨 ビルド状況

### Rust
- [ ] `cargo build --release`: 成功 / 失敗
- [ ] `cargo build --workspace`: 成功 / 失敗
- [ ] 所要時間: （記録）
- [ ] 成果物: `target/release/diffx` 存在: Yes / No

**ビルド出力**:
```
（_old/build-output.txt から重要部分をコピー）
```

**問題点**:
```
（あれば記載）
```

## 🧪 テスト状況

### cargo test --workspace
- [ ] 実行: 成功 / 失敗
- [ ] 通過したテスト数: （記録）
- [ ] 失敗したテスト数: （記録）

**テスト出力サマリー**:
```
（_old/test-output.txt から重要部分をコピー）
```

**失敗したテスト**:
```
（あれば記載）
```

### 個別モジュールテスト
- [ ] diffx-core: 成功 / 失敗
- [ ] diffx-cli: 成功 / 失敗
- [ ] diffx-python: 成功 / 失敗
- [ ] diffx-js: 成功 / 失敗

## 📦 バージョン情報

### Cargo.toml
```
workspace.package.version: （記録）
diffx-core: （記録）
diffx-cli: （記録）
diffx-python: （記録）
diffx-js: （記録）
```

### 実際のCLI
```bash
$ ./target/release/diffx --version
（出力を記録）
```

### 一致確認
- [ ] Cargo.tomlとCLIバージョンが一致: Yes / No

## 🎯 CLI動作確認

### 基本動作
```bash
$ ./target/release/diffx --version
（出力を記録）

$ ./target/release/diffx --help
（主要部分を記録）
```

### 簡単な差分テスト
```bash
# テスト内容
echo '{"a": 1}' > test1.json
echo '{"a": 2}' > test2.json
./target/release/diffx test1.json test2.json

# 結果
（出力を記録）
```

- [ ] 動作: 成功 / 失敗
- [ ] 期待通りの出力: Yes / No

## 📋 サポートフォーマット確認

実際にテストして動作確認したもののみ✓を付ける。

### JSON
- [ ] 基本的なdiff: 動作確認済み
- [ ] ネストした構造: 動作確認済み
- [ ] 配列: 動作確認済み

**テスト例**:
```bash
（実際に試したコマンド）
```

### YAML
- [ ] 基本的なdiff: 動作確認済み
- [ ] ネストした構造: 動作確認済み
- [ ] 配列: 動作確認済み

**テスト例**:
```bash
（実際に試したコマンド）
```

### TOML
- [ ] 基本的なdiff: 動作確認済み
- [ ] セクション: 動作確認済み
- [ ] 配列: 動作確認済み

**テスト例**:
```bash
（実際に試したコマンド）
```

### XML
- [ ] 基本的なdiff: 動作確認済み
- [ ] 属性: 動作確認済み
- [ ] ネスト: 動作確認済み

### INI
- [ ] 基本的なdiff: 動作確認済み
- [ ] セクション: 動作確認済み

### CSV
- [ ] 基本的なdiff: 動作確認済み
- [ ] ヘッダー: 動作確認済み

## 🔧 オプション動作確認

実際にテストして動作確認したもののみ記録。

### 基本オプション
- [ ] `--output <FORMAT>`: 動作確認済み
  - [ ] json: 動作
  - [ ] yaml: 動作
  - [ ] text: 動作
- [ ] `--color <WHEN>`: 動作確認済み
- [ ] `--quiet`: 動作確認済み
- [ ] `--verbose`: 動作確認済み

### 高度なオプション
- [ ] `--context <N>`: 動作確認済み
- [ ] `--ignore-case`: 動作確認済み
- [ ] `--ignore-whitespace`: 動作確認済み
- [ ] その他: （記録）

## 🐍 Python版の状況

### ビルド
```bash
cd diffx-python
maturin develop
```
- [ ] 成功 / 失敗

### インポート
```python
import diffx_python
```
- [ ] 成功 / 失敗

### 基本動作
```python
（実際に試したコード）
```
- [ ] 動作確認済み / 未確認

## 📦 npm版の状況

### ビルド
```bash
cd diffx-js
npm install
npm run build
```
- [ ] 成功 / 失敗

### インポート
```javascript
const diffx = require('diffx-js');
```
- [ ] 成功 / 失敗

### 基本動作
```javascript
（実際に試したコード）
```
- [ ] 動作確認済み / 未確認

## 🚨 発見された問題

### 致命的な問題
```
（ビルドできない、基本機能が動かないなど）
```

### 重要な問題
```
（一部の機能が動かない、パフォーマンス問題など）
```

### 軽微な問題
```
（ドキュメント不一致、警告など）
```

## ✅ 確実に動く機能

ここには**実際に試して動作確認した機能のみ**を記載。

### コア機能
1. （記録）
2. （記録）

### CLI機能
1. （記録）
2. （記録）

### Python API
1. （記録）
2. （記録）

### JavaScript API
1. （記録）
2. （記録）

## 📊 パフォーマンス

### ビルド時間
- Release build: （記録）秒
- Debug build: （記録）秒

### 実行速度
```bash
# 小さなファイル（<1KB）
（記録）

# 中程度のファイル（~100KB）
（記録）

# 大きなファイル（>1MB）
（記録）
```

## 🔍 依存関係

### Cargo.toml の依存
```
（主要な依存関係を記録）
```

### 問題のある依存
```
（非推奨、脆弱性、ビルド失敗など）
```

## 📝 次のステップ

### 修正が必要
1. （問題点から抽出）
2. （問題点から抽出）

### 仕様として固める
1. 動作確認できた機能を仕様書に記載
2. 動作しなかった機能はTODOに記載
3. ドキュメント作成の基礎資料とする

## 🎯 結論

### 使える状態か？
- [ ] Yes - 基本機能は動作する
- [ ] Partial - 一部は動作する
- [ ] No - 大幅な修正が必要

### プロジェクトの健全性
```
（総合的な評価）
```

### リブート方針
```
（このground-truthに基づいた方針）
```

---

**記録日**: 2025-11-14
**記録者**: （実行者）
**信頼性**: このドキュメントに記載された情報は実際に確認済み

**更新ルール**:
- 新しい確認をしたら追記
- 推測は書かない
- 嘘は絶対に書かない
- わからないことは「未確認」と書く
